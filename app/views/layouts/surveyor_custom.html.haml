!!!
%html{:lang => "en"}
  %head
    = render "layouts/head"
    = surveyor_includes
    = yield(:javascript)

  %body
    = render "shared/nav"

    .container.content-container
      = render "shared/flash_messages"
      = yield

    = render 'shared/footer'